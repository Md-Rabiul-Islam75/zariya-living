// Shared easing curve for the site's motion language (a soft "ease-out-expo").
// Typed as a fixed 4-tuple so Framer Motion accepts it as a BezierDefinition.
export const EASE: [number, number, number, number] = [0.22, 1, 0.36, 1];
